from https://github.com/abhishekti7/UnicornFilePicker
modified so that it can read getExternalFilesDir(null) (permissionless safe app storage, not actually all that external, but it can be read from a PC).
